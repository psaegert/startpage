'use strict';

const e = React.createElement;

class WeatherIcons extends React.Component {
  constructor(props) {
    super(props);
    this.state = { liked: false };
  }

  render() {
    if (this.state.liked) {
      return 'You liked this.';
    }

    return e(
      'button',
      { onClick: () => this.setState({ liked: true }) },
      'Like'
    );
  }
}

const domContainer = document.querySelector('#weather-icons');
ReactDOM.render(e(WeatherIcons), domContainer);